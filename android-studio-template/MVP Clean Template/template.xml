<template format="4"
        revision="1"
        name="MVP Clean Architecture"
        description="Creates a MVP Clean Architecture to make your life much easier!">

    <category value="Architectures"/>

    <!-- Feature Name -->
    <parameter
        id="featureName"
        name="Feature Name"
        type="string"
        constraints="class|unique|nonempty"
        default="FeatureName"
        help="The name of the activity class to create" />

    <!-- Activity XML Name -->
    <parameter
        id="mainActivityLayout"
        name="Layout Name"
        type="string"
        constraints="layout|unique|nonempty"
        default="main_activity"
        help="The name of the layout to create for the activity" />

    <!-- Package Name -->   
    <parameter
        id="packageName"
        name="Package name"
        type="string"
        constraints="package"
        default="com.mycompany.myapp" />

    <execute file="recipe.xml.ftl" />
</template>